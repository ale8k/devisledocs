/**
 * This middleware looks at the detailText string
 * and specifically pulls out custom syntax in our 
 * string to bemconverted into a new html5 based string.
 * I.e., it adds actual html into our string so we do not have to 
 * when saving :).
 */